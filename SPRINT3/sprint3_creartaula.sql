# id, iban, pan, pin, cvv, expiring_date
CREATE TABLE credit_card (
	id VARCHAR (50),
    iban VARCHAR (150),
    pan VARCHAR (100),
    pin VARCHAR (25),
    cvv VARCHAR (25),
    expiring_date VARCHAR (50)    
    );
    

